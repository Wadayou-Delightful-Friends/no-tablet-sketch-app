# AGENTS.md

AI エージェント（および人間）がこのリポジトリで作業するときの約束事。

## 最初に読むもの

- [README.md](README.md) — 画面構成・ディレクトリの手引き・開発コマンド
- [docs/README.md](docs/README.md) — 行動指針と、ルール・手順書の置き場所

## 変更の原則

### 頼まれたことだけをする

- タスクに必要なファイルだけを触る。無関係な整形・リネーム・リファクタを混ぜない
- 「ついでに直す」は禁止。気づいた問題は変更せず、提案として報告する

### 先回りしない

- 呼び出し元のない関数・使われない型・「後で使うかも」の抽象は作らない
- 共通化・interface 化は、同じコードが 2 回書かれてから検討する
- 空フォルダは予約席。タスクと無関係に埋めない
- 新しいフォルダ・ファイルの追加は、そのタスクで本当に必要なときだけ

### 小さく保つ

- 差分は最小に。1 コミット = 1 目的
- ファイルが肥大化してきたら、勝手に分割せず提案する

## コーディングスタイル

- 変数・関数の名前は、多少長くても用途が具体的にわかるものにする
- リーダブルコード（読み手の理解時間を最小化する）を意識する
- 判断基準の詳細は [docs/rules/coding-style.md](docs/rules/coding-style.md)

## 依存関係

- パッケージの追加・更新・削除は事前に相談する。勝手に `pnpm add` しない

## 完了の条件

- `pnpm build` と `pnpm lint` が通ること
- 変更したファイルの一覧を報告する。スコープ外と判断して触らなかったことがあれば、それも添える
- ビルドやテストが失敗したまま「完了」と報告しない

## コミット

- メッセージは日本語、`feat:` / `fix:` などの接頭辞を付ける（既存ログに合わせる）
